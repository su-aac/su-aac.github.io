---
const isEnglish = Astro.url.pathname.startsWith('/en');
---

<p class="odyssey-logo">{isEnglish ? 'AAC Action Now' : 'AAC今天就開始'}</p>

<style>
	.odyssey-logo {
		width: fit-content;
		margin: 0;
		font-family: var(--theme-font-family-serif);
    font-size: var(--font-size-md);
		color: inherit;
	}
	.odyssey-logo:hover {
		text-decoration: underline;
		cursor: pointer;
	}
</style>
